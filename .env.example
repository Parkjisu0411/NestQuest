# Copy to .env.local, then enter values locally. Never commit .env.local.
# These runtime keys are intentionally embedded in personal builds (including web dist).
# Public data: Decoding key. Kakao REST: addresses. Kakao JavaScript: map display.
NESTQUEST_PUBLIC_DATA_KEY=
NESTQUEST_KAKAO_REST_KEY=
NESTQUEST_KAKAO_JAVASCRIPT_KEY=
